spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: test

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jump.toolsshop.model

logging:
  level:
    root: info